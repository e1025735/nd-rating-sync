{
  "name": "nd-rating-sync",
  "version": "0.2.0",
  "description": "Reads embedded rating tags from music files (FMPS_Rating / POPM / iTunes rtng) and syncs them to Navidrome's user ratings via the Subsonic API.",
  "author": "Your Name <you@example.com>",
  "homepage": "https://github.com/yourusername/nd-rating-sync",
  "capabilities": ["lifecycle", "schedulerCallback"],
  "permissions": {
    "library": {
      "reason": "Read music files to extract embedded rating tags",
      "filesystem": true
    },
    "subsonicapi": {
      "reason": "Call setRating to write star ratings back into Navidrome"
    },
    "users": {
      "reason": "Required by the subsonicapi permission for per-user authorisation"
    },
    "scheduler": {
      "reason": "Schedule periodic scans of the music library"
    }
  },
  "config": [
    {
      "key": "username",
      "label": "Navidrome username",
      "description": "The Navidrome account whose ratings will be updated. Make sure this user is also added to 'User Access' below.",
      "type": "string",
      "required": true
    },
    {
      "key": "sync_schedule",
      "label": "Sync schedule (cron expression) [admin]",
      "description": "Cron expression for automatic recurring scans. Examples: '0 */6 * * *' = every 6 hours, '0 2 * * *' = daily at 02:00. Changes take effect after the plugin is reloaded. Only administrators should change this.",
      "type": "string",
      "default": "0 */6 * * *"
    },
    {
      "key": "user_scan_cooldown_hours",
      "label": "User-scan cooldown (hours) [admin]",
      "description": "Minimum number of hours a user must wait between manual scan requests. Default is 24 (once per day). Set to 0 to disable the cooldown. Only administrators should change this.",
      "type": "string",
      "default": "24"
    },
    {
      "key": "trigger_user_scan",
      "label": "Trigger scan now",
      "description": "Set to 'true' to request an immediate one-time rating scan. The plugin checks for this request every 15 minutes. It resets automatically once the scan is queued. Subject to the cooldown period above.",
      "type": "string",
      "default": "false"
    },
    {
      "key": "skip_already_rated",
      "label": "Skip already-rated songs",
      "description": "When 'true' (default), songs that already have a star rating in Navidrome are left untouched. Set to 'false' to overwrite existing ratings from file tags.",
      "type": "string",
      "default": "true"
    },
    {
      "key": "max_songs_per_run",
      "label": "Max songs per scan",
      "description": "Maximum number of songs to process in a single scheduled run. Use 0 for no limit. Defaults to 500.",
      "type": "string",
      "default": "500"
    }
  ]
}
